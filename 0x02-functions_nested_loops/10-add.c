#include "main.h"

/**
 * add - adds two numbers together and return the answer
 * @a: number to be added to b
 * @b: number to be added to a
 * Return: returns result
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
